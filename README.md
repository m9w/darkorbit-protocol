# darkorbit-protocol

## Installation

Add JitPack repository in your root `build.gradle.kts`:

```kotlin
repositories {
    maven { url = uri("https://jitpack.io") }
}

dependencies {
    implementation("com.github.m9w:darkorbit-protocol:latest")
}
```
